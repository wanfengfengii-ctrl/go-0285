# 装配式墙板灌浆套筒临时支撑卸除质量闭环

## 项目目标

构建一个以逻辑时钟驱动、可持久化恢复的 Go HTTP 后端，管理预制墙板从任务锁定、材料核验、定位封缝、浆料拌制、顺序灌注、试件养护、饱满度检测、缺陷补灌、双人复核到卸撑裁定的完整流程。核心状态是不可覆盖的“墙板—套筒—端口”证据图：每次有效动作追加带任务代次、逻辑时间、操作者和幂等操作号的事件；补灌只能创建新代次，旧证据和迟到回执永久保留但不能覆盖当前结论。采用文件型追加日志与原子快照实现真实持久化、崩溃恢复和确定性重放。工程限定为约 20—28 个生产 Go 文件、2000—3000 行有效生产代码且目标约 2500 行，分布于 cmd、domain、application、persistence、devices、httpapi 等至少六个有实际职责的包；公开测试不少于四个文件和十二个测试用例。仓库须包含 go.mod、可运行入口、健康检查、优雅停机，以及可构建 linux/amd64 与 linux/arm64 镜像的 Dockerfile/buildx 配置。

## 端到端业务流程

1. 任务锁定：提交楼栋层段、墙板定位摘要、墙板编号、钢筋与套筒规格清单、灌浆口/出浆口有向拓扑、材料批次、理论容积、损耗上限、工作时限、试件、检测测线、阈值和初始代次；服务在单事务内校验目录版本与规则摘要，成功后生成不可变锁定清单，任一错配则整项拒绝。
2. 施工准备与拌合：依序登记材料证明核验、墙板定位和接缝封堵；获取搅拌设备租约后，以整数克、毫升和定点比例登记投料、配合用水、拌合损耗及试件取样，形成带到期逻辑时刻和可消耗体积池的拌合盘；流动度设备失败仅产生待重试调用。
3. 连续灌注：施工方原子获取灌浆泵租约并预留浆料体积，按锁定拓扑逐端口登记进浆、稳定出浆、压力证据、出浆口封堵和换口；记录器只允许扩展连续前缀，越序、重复、错误代次、租约过期或超过工作时限均不产生有效证据，失败时同时回滚体积预留和租约变更。
4. 养护与检测：试件按锁定养护计划登记强度结果；超声测线和内窥孔位须先取得检测通道租约。设备拒绝、断连、超时或格式错误被持久化为具有逻辑时间、确定退避时刻与有限重试序号的调用记录，只有合法成功回执可形成检测证据。
5. 缺陷传播与补灌：强度不足、超声疑似空腔、内窥缺浆或封堵泄漏触发一次确定性计算，沿同一浆路以及锁定的相邻套筒边生成排序、去重的复检集合；仲裁器隔离当前代次，并以比较交换方式创建唯一新代次。补灌复用材料、租约、前缀和检测流程，但不修改旧代次事件。
6. 双人复核与终局：当当前代次所有套筒前缀完整、材料守恒成立、强度和饱满度证据闭合且补灌复检完成后，两名不同且资格有效的人员分别签署固定审查摘要；卸撑、质量隔离和取消通过同一单写终局比较交换并发竞争，唯一胜者产生终局记录，只有卸撑胜者获得唯一凭据。

## 核心组件与职责

1. 墙板连接与材料规则目录：维护带版本的钢筋—套筒兼容矩阵、批次证明、用水规则、容积和损耗边界、人员资格；生成稳定规则摘要，并为锁定事务提供只读快照和陈旧版本检测。
2. 灌浆检验任务聚合：拥有任务阶段、不可变锁定清单、代次、设备调用、复核与终局状态；执行命令级幂等校验、阶段守卫和事件追加，并通过存储端条件写保证单任务串行提交。
3. 浆料体积及资源租约管理器：使用整数定点算法维护每盘投料、加水、损耗、取样、预留、实灌和退回量；原子管理泵、搅拌设备及检测通道的获取、续租和释放，并以逻辑时钟判断过期。
4. 灌注前缀与端口证据记录器：保存墙板—套筒—端口证据图和每代灌注前缀，校验端口唯一性、拓扑连通性、出浆稳定、封堵及换口顺序，所有历史证据只追加不可覆盖。
5. 强度饱满度复检及终局仲裁器：验证试件、超声、内窥和泄漏证据，计算确定性缺陷传播集合，隔离迟到代次回执，核验双人复核，并原子裁定卸撑、质量隔离或取消。
6. Go HTTP API：提供 JSON 命令、查询、设备回执和恢复状态接口；统一校验请求、映射稳定错误码、输出规范排序结果，并接入文件事件库、原子快照、启动重放和优雅停机。

## 领域规则与不变量

1. 任务一旦锁定，定位摘要、连接清单、端口拓扑、材料及用水批次、理论容积、损耗上限、工作时限、试件、测线、阈值和代次均不可原地修改；变更只能取消后新建任务。每根钢筋恰好对应一个规格相容的套筒，每个套筒必须具有目录规定的唯一灌浆口和出浆口，拓扑必须连通且无环断裂。
2. 所有质量与体积计算使用明确量纲的 int64 定点整数：质量为克、体积为毫升、比例按百万分比表示。乘法先检查溢出，再按统一半向上规则舍入；负数、零分母、超过损耗上限或理论容积边界均拒绝。每盘必须满足可用量=投料折算体积+加水量-拌合损耗-试件取样量，且可用量=未分配量+已预留量+已实灌量；任何失败命令不得破坏等式。
3. 资源租约键由资源类型和资源编号组成，同一逻辑时刻只能有一个有效持有者。获取、续租、释放、浆料预留和扣减必须在一个持久化事务内比较版本；到期边界采用 now>=expiresAt 即过期。相同操作号与规范化内容摘要相同则返回原结果，摘要不同则返回 IDEMPOTENCY_CONFLICT。
4. 每代灌注按锁定浆路形成事件序列：灌浆口开始、对应出浆口稳定、出浆封堵、换至下一端口。只有下一合法事件可以扩展前缀；重复事件不能被当作幂等，除非操作号和内容完全一致。浆料盘在事件逻辑时刻达到或超过工作时限、泵租约无效、体积不足或代次非当前时，均不得写入证据。
5. 设备调用以调用号和请求摘要标识。拒绝、断连、超时或格式错误只追加失败尝试及 nextRetryAt，重试序号从一递增并按固定表计算退避；成功回执必须匹配设备、任务、目标、代次和调用号。旧代次或已被成功回执关闭的迟到结果仅归档，不产生有效读数。
6. 复检传播以有序邻接表执行：从缺陷套筒出发，包含同一浆路后继端口所涉套筒及其直接相邻套筒，合并多缺陷起点后按楼层、墙板、套筒、端口、试件、代次排序并去重。一个被隔离代次只能比较交换创建一个后继补灌代次，新代次重新拥有独立前缀和结论。
7. 复核人员必须资格有效、互不相同，且两次复核绑定同一当前证据摘要；证据新增后旧复核不再满足放行条件。卸撑凭据仅在所有当前证据闭合、守恒校验通过且双人复核完成时生成一次。卸撑、质量隔离和取消共享一个不可逆终局槽。
8. 所有业务拒绝使用稳定错误码，包括 WALL_PANEL_MISMATCH、SOCKET_INCOMPATIBLE、STALE_RULE_DIGEST、INVALID_TOPOLOGY、MATERIAL_IMBALANCE、FIXED_POINT_OVERFLOW、LEASE_CONFLICT、LEASE_EXPIRED、SLURRY_EXPIRED、VOLUME_OVERCLAIM、PREFIX_VIOLATION、PORT_SEAL_OUT_OF_ORDER、DEVICE_RETRY_PENDING、GENERATION_CONFLICT、IDEMPOTENCY_CONFLICT、REVIEWER_CONFLICT、EVIDENCE_INCOMPLETE 和 TERMINAL_CONFLICT；批量原因必须采用规定的领域键顺序输出。

## 数据模型与持久化

1. CatalogSnapshot：规则版本、摘要、钢筋/套筒兼容项、材料证明、用水约束、损耗与工作时限边界、设备及人员资格。
2. InspectionTask：任务 ID、楼栋、层段、墙板、锁定摘要、阶段、当前代次、聚合版本、终局槽、创建和更新逻辑时间。
3. LockedPlan：墙板定位摘要、连接列表、端口节点与有向边、浆路顺序、材料及用水批次、理论毫升数、损耗上限、试件计划、超声测线、内窥孔位和放行阈值。
4. MixBatch 与 VolumeLedger：拌合盘 ID、任务代次、投料克数、加水毫升、损耗毫升、取样毫升、可用/未分配/预留/实灌毫升、工作时限和账本版本。
5. Lease：资源键、持有任务、操作号、获得时刻、到期时刻和版本；检测测线以通道资源键参与互斥。
6. EvidenceEvent：事件 ID、任务、墙板、套筒、端口或试件、代次、类型、定点值、逻辑时间、操作者、操作号、内容摘要及有效性状态；历史事件不可更新或删除。
7. GenerationState：代次、父代次、原因、端口连续前缀、缺陷集合、确定性复检范围、强度/超声/内窥/泄漏结论和隔离状态。
8. DeviceCall、Review 与 TerminalDecision：脚本化设备尝试和重试计划、复核人员与证据摘要、唯一终局类型及卸撑凭据摘要。持久层另存 EventEnvelope、IdempotencyRecord、原子快照版本和校验和。

## 公开接口

1. POST /api/v1/tasks 与 POST /api/v1/tasks/{id}/lock：创建并锁定任务；锁定请求携带目录版本和规则摘要，返回完整排序校验原因。
2. POST /api/v1/tasks/{id}/commands：统一提交定位、封缝、拌合、取样、租约、浆料预留、灌注端口事件、检测、补灌、复核及终局命令；请求头 Idempotency-Key 必填，响应含聚合版本和逻辑时间。
3. POST /api/v1/device-calls/{callID}/attempts：接收脚本化成功、拒绝、断连、超时或格式错误结果；生产实现通过设备适配器调用，测试实现使用确定脚本。
4. GET /api/v1/tasks/{id}、/evidence、/generations 和 /ledger：查询任务、不可覆盖证据图、历代补灌状态和浆料账本；列表统一按领域键稳定排序。
5. GET /api/v1/resources/{type}/{id}/lease 与 GET /api/v1/tasks/{id}/decision：查询当前有效租约及终局/卸撑凭据；GET /healthz 和 /readyz 报告事件日志、快照及恢复状态。
6. 进程入口接受事件日志路径、快照路径和逻辑时钟模式配置；测试直接调用应用服务并注入手动时钟、同步屏障、故障脚本和临时持久化目录，避免依赖真实时间与网络。

## 失败边界

1. 锁定、租约与体积、灌注前缀、设备成功证据、补灌建代及终局均以单个聚合版本的条件追加事务提交；先在内存副本完成全部校验，再写临时事务段、同步落盘并原子发布。失败或进程中断时，恢复器丢弃未提交事务段，不得留下部分扣量、租约、前缀、证据或凭据。
2. 事件日志每条记录包含长度、序号和校验和；启动时只接受完整已提交事务，校验损坏、序号间隙或快照摘要不匹配则进入只读未就绪状态并返回 PERSISTENCE_CORRUPT，不默默跳过。快照通过临时文件、fsync 和原子重命名生成，日志始终可独立重放。
3. 同一任务使用持久化乐观版本控制，不同任务可并发；共享浆料盘和资源租约使用各自版本参与同一事务。版本冲突统一重读并按有限次数确定重试，耗尽后返回 CONCURRENT_MODIFICATION，绝不依赖非确定随机退避。
4. 设备边界绝不把传输成功等同于业务读数成功；格式、单位、目标、代次和时限分别校验。失败尝试可持久化，但不得进入有效证据集合或推进阶段。
5. 定点计算在改变账本前完成负数、加减乘、换算、舍入和除零检查；任一中间值溢出即整条命令失败。批量命令先收集并稳定排序全部原因，再统一拒绝。
6. 终局发布与卸撑凭据生成属于同一条件事务；即便卸撑、质量隔离和取消由同步屏障同时释放，也只有一个事务可占据终局槽，败者只得到 TERMINAL_CONFLICT。

## 验收标准

1. 锁定操作固定全部规定字段及任务代次；墙板或套筒错配、陈旧证明/摘要、重复端口、断裂拓扑、非法理论容积或材料批次会整项拒绝，原因按领域键确定排序且重启后结果一致。
2. 整数定点账本正确处理投料、用水、损耗、取样、预留和实灌并始终守恒；公开边界用例证明负值、半向上舍入、除零和 int64 溢出均返回稳定错误且事务无部分扣减。
3. 泵、搅拌设备和检测通道的获取、续租、释放及浆料扣减在并发屏障下原子裁定；同操作号同内容重试返回原结果，内容冲突、租约过期和体积超领不改变状态。
4. 每个套筒只能按锁定拓扑推进连续灌注前缀；越序封堵、重复端口、错误代次、错误浆路和工作时限到期边界均不写入有效证据，成功序列重启后可完整恢复。
5. 四类检测设备的拒绝、断连、超时和格式错误只形成带逻辑时间及确定重试次数的待重试调用；无有效读数、前缀或检测结论被伪造，迟到回执不会覆盖新代次。
6. 缺陷依据同一浆路与直接相邻套筒规则一次性生成排序去重的复检集合；并发补灌请求只创建一个新代次，旧代次证据、回执和审计轨迹仍可查询。
7. 仅在灌注、守恒、强度、饱满度、补灌复检与两名不同合格人员复核全部闭合时生成唯一卸撑凭据；卸撑、隔离、取消并发竞争只有一个持久化终局。
8. 仓库可通过 go test ./... 与竞态检测，公开测试分布于至少四个测试文件且不少于十二例；生产代码包含至少二十个 Go 文件、四个以上实质包并保持约 2000—3000 有效行，同时提供 go.mod、启动说明及 linux/amd64、linux/arm64 Docker 构建支持。

## 确定性测试场景

1. 拓扑锁定表驱动测试：固定虚构楼栋、墙板、钢筋、套筒和端口图，覆盖相容成功、墙板错配、套筒不相容、重复端口、缺失出浆口、断裂浆路及稳定原因排序。
2. 目录陈旧测试：锁定请求使用旧材料证明版本或错误规则摘要，断言 STALE_RULE_DIGEST 且任务未进入锁定态。
3. 定点守恒表驱动测试：使用整数毫升浆料表验证正常守恒、取样和损耗、半向上舍入、负值、除零、理论容积越界及乘法溢出。
4. 浆料超领并发测试：两个任务在同步屏障后竞争同一拌合盘剩余体积，精确断言唯一胜者、失败码、账本版本和重启后的守恒等式。
5. 资源租约竞争测试：并发获取同一灌浆泵和交叉占用同一超声测线，覆盖续租、错误持有人释放、now==expiresAt 边界及无残留租约。
6. 灌注前缀测试：按固定端口序列推进，覆盖出浆口越序封堵、重复动作、换口跳跃、错误代次和完整前缀；逐步精确比较证据事件。
7. 工作时限测试：手动逻辑时钟停在到期前一刻和到期时刻，断言前者可灌注、后者返回 SLURRY_EXPIRED 且预留体积与前缀均回滚。
8. 设备故障脚本测试：依次注入拒绝、断连、超时、格式错误和成功，验证确定重试序号、nextRetryAt、无伪造读数以及成功后关闭调用。
9. 复检传播测试：对同一浆路多个缺陷起点及相邻套筒输入固定图，断言一次性生成的套筒、端口、试件和测线集合完全匹配且有稳定顺序。
10. 补灌代次隔离测试：并发创建补灌代次只成功一次；随后提交旧代次迟到超声回执，断言其可审计但不改变当前结论。
11. 幂等与回滚测试：同操作号同内容重复得到完全相同响应，不同内容返回 IDEMPOTENCY_CONFLICT；注入持久化提交失败后重启，断言无部分扣量、前缀、有效证据或凭据。
12. 卸撑门禁与终局竞争测试：逐项缺失强度、饱满度、复检或双人资格时拒绝；条件闭合后用屏障并发发起卸撑、质量隔离和取消，精确断言唯一终局及至多一个卸撑凭据。
13. 恢复一致性测试：在锁定、拌合、灌注、补灌和复核后分别重启，比较重放前后的规范化任务、账本、证据图、幂等结果和终局摘要。

## 组件追踪关系

1. 墙板连接与材料规则目录对应锁定流程、LockedPlan/CatalogSnapshot、WALL_PANEL_MISMATCH、SOCKET_INCOMPATIBLE、STALE_RULE_DIGEST 和验收 1。
2. 灌浆检验任务聚合贯穿所有业务流，承载任务阶段、幂等记录、聚合版本、事件日志与恢复，对应验收 1、3、6、7。
3. 浆料体积及资源租约管理器对应拌合和连续灌注流程、MixBatch/VolumeLedger/Lease、固定点溢出与并发原子性，对应验收 2、3。
4. 灌注前缀与端口证据记录器对应连续灌注流程、EvidenceEvent/GenerationState、拓扑和工作时限错误，对应验收 4。
5. 强度饱满度复检及终局仲裁器对应养护检测、缺陷补灌和双人复核流程，承载 DeviceCall/Review/TerminalDecision，对应验收 5、6、7。
6. Go HTTP API 暴露全部命令与查询并统一错误排序，连接持久层、逻辑时钟及设备适配器，同时承担健康检查、重启恢复和双架构部署，对应验收 8。

## 独特性

本项目聚焦装配式混凝土墙板的套筒灌浆与临时支撑卸除，不复用梁蒸养、钢结构螺栓节点或一般材料放行模型；其辨识度来自端口拓扑连续前缀、按拌合盘计时的可消耗浆料池、跨浆路邻接传播的复检集合、不可覆盖的多代证据图，以及卸撑、质量隔离、取消共享的单写终局。
